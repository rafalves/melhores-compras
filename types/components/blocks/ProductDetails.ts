// ProductData is just a placeholder to know where in the page order
// we need to display all descriptions of the ProducTable

export interface ProductDetails {
  id: number,
  __component: string,
  order?: number
}